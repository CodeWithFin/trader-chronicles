'use client'

/** Shared chart colors for the Family light theme. */
export function useChartPalette() {
  return {
    dark: false,
    tick: '#343433',
    tickMuted: '#7e7e7d',
    axis: '#e5d5c3',
    grid: '#f2f0ed',
    tooltipBg: '#ffffff',
    tooltipBorder: '#f2f0ed',
    tooltipLabel: '#343433',
    line: '#121212',
    dotFill: '#ffffff',
    accent: '#ff3e00',
    positive: '#00c978',
    negative: '#ff2b3a',
  }
}
