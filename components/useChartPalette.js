'use client'

/** Shared chart colors for the light interface. */
export function useChartPalette() {
  return {
    dark: false,
    tick: '#000000',
    tickMuted: '#374151',
    axis: '#111827',
    grid: '#e5e7eb',
    tooltipBg: '#ffffff',
    tooltipBorder: '#000000',
    tooltipLabel: '#111827',
    line: '#111827',
    dotFill: '#ffffff',
  }
}
